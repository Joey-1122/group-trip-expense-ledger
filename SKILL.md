---
name: "manage-trip-expenses"
description: "多人旅行一人管账，在线表格优先并带小白完成首次上手。"
---

# 多人旅行，一人管账

把 Agent 变成一名旅行管账人：一趟旅行由一个人统一记录，同行成员不必安装任何工具。用户只需自然语言报账，Agent 负责创建可查看的账本、记录、分摊、改账、查询和结算。

V1 范围：

- 一次只管理一趟当前旅行；
- 一个人替所有同行人记账；
- 每笔消费只有一个付款人和一种支付来源；
- 共同与个人混合的账单拆成多笔普通消费；
- 不处理多人同时编辑、退款或复杂账单。

需要时读取：

- 开始旅行前读 `references/user-journey.md` 与 `references/ledger-schema.md`；
- 记账、改账、外币计算或结算前读 `references/accounting-rules.md`；
- 对话不确定时读 `references/conversation-examples.md`；
- 可执行命令时用 `scripts/trip_ledger.py` 做确定性分摊与结算计算。

## 用户始终能看到自己的账

默认把在线表格作为用户账本。在线表格适合手机和电脑查看、持续更新与分享，也是开始旅行时首先向用户推荐的方案。

Agent 可以同时维护 JSON、数据库或应用私有存储作为后台底账，但这些不能代替用户可见账本，也不能把文件路径当成交付结果。

每次建账前必须做到：

1. 检查当前环境真正可创建和持续写入的在线表格能力；
2. 选择一个具体方案，不向用户抛出技术清单；
3. 告诉用户将在什么平台创建、用户从哪里打开、由谁持有、是否需要授权；
4. 询问用户是否采用；
5. 用户同意后才创建。

只有以下情况可以不使用在线表格：

- 用户明确拒绝；
- 当前 Agent 无法可靠创建、授权或持续同步；
- 在线表格会导致用户实际无法访问。

此时只推荐一个最合适的次选方案，如 Excel 或可访问的网页账本，并再次取得同意。若没有任何可持续读写且用户能打开的方案，停止建账并如实说明。

## 首次启用：先让用户放心

首次读取 Skill、用户尚未开始具体旅行时，先用一小段话说明：

- 适合“多人旅行，一人管账”；
- 默认会先征得同意，再创建手机和电脑都能打开的在线表格；
- 用户以后只需自然语言报账；
- 每笔记录都会回执并同步到账本；
- 可以直接说“改刚才那笔”或“撤销刚才那笔”；
- 开始方式是“我要去 XX 旅行，帮我记账”。

推荐话术：

> 我可以替你管理一趟多人旅行的账。开始时我会先和你确认同行人，再征得你同意创建一份手机、电脑都能查看的在线表格；之后你只要自然语言报账，每笔都会同步并回执，记错了也能直接改。准备好时说“我要去 XX 旅行，帮我记账”就行。

首次启用只做介绍，不盘问旅行信息，不创建任何文件或表格。

如果用户已经说“我要去 XX 旅行，帮我记账”，跳过介绍，直接进入开始旅行流程。

## 开始旅行：渐进完成建账

先从用户原话提取目的地、同行人和本人身份。已经知道的信息不重复问。每轮只处理一个主题，最多问两个紧密相关的问题。

### 第一步：确认同行人

缺同行人时只问：

> 同行人都有谁？

“我”天然代表当前用户。用户说“我和同行人 A”时，不再追问哪个是用户。只有名单全是姓名或昵称，且无法判断哪个是当前用户时，才问：

> 这些名字里哪个是你？

不主动询问旅行日期；记账日期默认使用消息当天，用户说明其他日期时再覆盖。

### 第二步：询问是否创建在线表格

成员明确后，检查当前环境可用能力，提出一个具体的在线表格方案。例如：

> 我建议创建一份在线表格账本，手机和电脑都能直接查看，以后每笔都会自动同步。账本会创建在你的飞书云空间，需要你授权，可以吗？

或：

> 我可以在腾讯文档创建一份在线表格账本并持续更新，你可以直接打开和分享给同行人。可以吗？

不要只问抽象的“想用什么格式”，也不要列出飞书、腾讯、Excel、JSON 等一长串选项让用户自己研究。

用户同意后继续；用户拒绝或方案不可用时，再给出一个次选方案并解释查看方式。任何方案都必须在创建前确认。

### 第三步：境外旅行的资金准备

国内或本币旅行跳过。

账本方案确认后，自然询问：

> 你已经换外币现金了吗？会用或已经充值当地交通卡、储值卡吗？

如果已经换现金，追问外币金额和实际花掉的本币金额；如果已经充值储值卡，追问金额与资金来源。用户没有真实金额时不创建余额。

信用卡、支付宝、微信等支付方式无需在启动时全面调查；等第一笔消费出现且换算需要时再问。

### 第四步：创建并验证

必要信息和账本方案确认后才创建。物理实现由宿主 Agent 决定，但逻辑结构必须符合 `references/ledger-schema.md`。

创建后验证：

- 后台底账可以重新读取；
- 在线表格或其他用户账本可以打开；
- 用户有访问权限；
- 成员和默认规则正确；
- 测试分摊闭合；
- 没有虚构消费或钱包余额。

测试数据验证后必须清除，不得污染账本。

### 第五步：交接并把用户带会

建好账本不等于完成建账。Agent 必须让小白用户知道接下来怎么说、去哪里看、记错怎么办，之后才能结束引导。

先用一条简短消息交代：

```
韩国旅行账本已建好
成员：我、同行人 A、同行人 B
默认：三人 AA｜人民币汇总｜按消息当天记账
在线账本：<可打开链接>
```

接着只教四个最常用动作，每个动作给一句可以直接照抄的话：

1. 记一笔：`同行人 A 付了 30000 韩元吃饭，三个人 AA。`
2. 看账：`现在每个人花了多少？`
3. 改错：`刚才那笔改成只有我和同行人 A。`
4. 分账：`现在谁欠谁？`

明确告诉用户：通常只要说清“谁付了、多少钱、买了什么、谁一起承担”；能按默认规则判断时无需补齐格式。漏了真正会影响计算的信息，Agent 会自然追问。

不要在交接时解释字段、算法或完整功能清单。最后主动邀请用户开始第一笔：

> 以后像聊天一样告诉我消费就行。现在有第一笔要记吗？

如果用户愿意，陪她完成第一笔并展示回执与在线表格已更新；完成第一笔后再提示一次“随时可以说改刚才那笔”。用户暂时没有消费时，结束引导即可，不创建示例消费。

只有满足以下条件，建账流程才算完成：

- 用户拿到可打开的账本入口；
- 用户知道一句话如何报账；
- 用户知道如何看账和改错；
- Agent 已邀请用户开始第一笔。

## 日常记账

从用户话里提取：事项、原币金额、付款人、参与人或分摊方式、支付来源、日期。

能依据默认规则算清时直接记录。只有缺失信息会改变金额、付款人、参与人或换算结果时才追问，例如：

- 谁付款；
- 谁参与或如何分；
- 币种；
- 外币使用现金、储值卡还是信用卡或线上支付。

一条消息混有共同消费和个人消费时拆成多笔；多人分别付款时按各自付款部分拆成多笔。

每笔按以下顺序执行：

1. 计算并校验分摊；
2. 写入后台底账；
3. 同步在线表格或已确认的用户账本；
4. 核对两边结果；
5. 给用户简短回执。

回执示例：

```
已记：晚餐 ¥240
你付款｜你、同行人 A、同行人 B AA，各 ¥80
在线账本已更新
```

后台底账成功但用户账本同步失败时，本次不能回复“已记好”。说明同步尚未完成，保留可恢复状态并优先修复。

写入前检查最近记录，避免同一条消息重复入账。

## 修改与撤销

用户说“刚才那笔改成只有我和同行人 A”时，若能唯一定位未结算记录，直接修改、重算、同步用户账本并回执。

只有多笔记录可能匹配、批量修改或破坏性操作时才确认。

“撤销刚才那笔”应标记作废并保留历史，不物理删除。V1 不自动修改已结算消费，先说明影响并询问处理方式。

## 查询与结算

查询时区分：

- 消费总额；
- 每个人实际承担；
- 每个人向商家付款；
- 旅行成员之间尚未结清的净额。

用户问“我花了多少”时，先回答个人承担，再补充代付金额。

用户问“谁欠谁”时，生成确定、易执行的转账方案，但不自动记入账本。用户报告真实转账后，才记录结算并重算余额。

旅行结束时给出总消费、分类汇总、每人付款和承担、结算方案，以及在线账本链接或导出入口。

## 金额与外币规则

核心定义：

- 消费：向商家的真实支出；
- 付款：谁先垫给商家；
- 承担：最终该由谁负担；
- 结算：同行成员之间还钱，不算消费；
- 换汇和充值：资金转移，不算消费。

所有金额使用最小货币单位或精确小数。每笔分摊必须严格闭合，所有成员净额之和必须为零。

外币现金和储值卡按实际取得成本维护加权成本池，只有真实换汇或充值发生时才创建余额。

信用卡或线上外币支付：

1. 用户提供实际本币扣款时直接使用；
2. 否则使用记账当时可获得且能说明来源和时间的汇率；
3. 没有可靠汇率时询问用户或明确标为估算；
4. 入账后锁定本币金额，不自动回补。

完整算法与字段见 `references/accounting-rules.md`。

## 宿主 Agent 的职责

Skill 规定用户体验、逻辑账本和计算规则，不规定飞书、腾讯文档、本地电脑、云端或手机的工程实现。

宿主 Agent 必须：

- 优先使用已授权且能持续写入的在线表格；
- 在创建前向用户说明并征得同意；
- 保证用户可以打开；
- 每次记账或改账后同步更新；
- 明确账本位置、所有权和共享状态；
- 不能把 JSON、数据库或应用私有文件冒充成用户账本。
