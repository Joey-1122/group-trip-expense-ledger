---
name: manage-trip-expenses
description: >
  Use when 用户要为一趟多人旅行记账分账、一个人替同行人管账，或说"我要去 XX 旅行帮我记账"、
  "记一笔"、"谁欠谁"、"我花了多少"、"AA"、"改刚才那笔"，或报出差旅消费、换汇、
  储值/交通卡(Suica/八达通/T-money 等)消费需要折算人民币时。跨宿主(OpenClaw 飞书/微信、Coze、WorkBuddy)。
---

# 多人旅行，一人管账

把 Agent 变成一名旅行管账人：一趟旅行由一个人统一记录，同行成员不必安装任何工具。用户只需自然语言报账，Agent 负责创建可查看的账本、记录、分摊、改账、查询和结算。

**核心原则**：LLM 负责理解自然语言，确定性规则负责算钱。金额、分摊、结算必须闭合可核对；理解层的模糊不能带进账本。

V1 范围：一次管一趟当前旅行；一个人替所有同行人记账；每笔消费一个付款人一种支付来源；共同与个人混合拆成多笔；不处理多人同时编辑、退款、并行旅行、复杂账单。

## 六条正向不变量（最重要，先读）

这六条从设计上让整类错误不发生。遵守它们，胜过一长串"小心 X"。

1. **写入前一行回显，写入后一行回执。** 每笔先复述关键理解（事项/金额/付款人/参与人）成一句"要记：…"，算完再给"已记：…"。用户永远先看到你怎么理解，再看到结果。→ 防相邻消息串账、记错对象。

2. **没有新消费事实的消息 = 提问，只回数字，绝不写库。** "我一个人餐饮花了多少""谁欠谁"是查询，用统计回答，不碰账本。**改账必须指向一条已有记录**（"刚才那笔""那笔午饭"）；定位不唯一就先列候选让用户选，不猜。→ 防把查账当改账。

3. **已入账的本币金额默认锁定不可改。** 每条消费有 `金额状态 = 锁定 | 待出账`。只有"待出账"记录接受汇率/金额更新；"锁定"记录要改，必须用户显式指名并确认。批量更新汇率时只动"待出账"，绝不回头改已锁定的。→ 防把算好的汇率又改一遍。

4. **回执自带重复提示。** 写入前用（当天 + 金额 + 付款人 + 事项）比对最近记录；命中相似项就在回执里问"今天已有一笔 X，仍要记吗"。去重是看得见的确认，不是静默入账。→ 防重复记录。

5. **账本自己持有规则。** 用户说的默认（"以后没说就是我和 Max""吃喝默认共同""人民币默认微信"）写进账本可见的「规则/设置」区，不靠脑记；可查看、可修改。记账时引用这些规则，不重新发明。→ 防规则靠记忆出错。

6. **trip profile 决定加载哪些模块。** onboarding 问清"从哪到哪"。**国内行**（中文提问且未说明出发地时默认从中国出发）只用本文件的单币种模型，全程不问外币。**国际行**才加载 `references/foreign-money.md`（钱包、资金池、实时汇率、储值卡）。→ 复杂度不下放给所有人。

## 钱的定义（锋利，不混）

- **消费**：向商家的真实支出（进统计）。
- **付款**：谁先垫给商家。
- **承担**：最终该由谁负担。
- **结算**：同行成员之间还钱——**不算消费**。
- **换汇 / 充值**：资金转移——**不算消费**。

分摊必须**严格闭合**（各人应承担之和 = 消费本币金额）；所有成员**净额之和 = 0**。金额用最小货币单位或精确小数。

## 首次启用：先让用户放心

首次读取 Skill、用户还没开始具体旅行时，只用一小段话介绍，**不建任何存储、不盘问旅行信息**：

> 我可以替你管理一趟多人旅行的账。开始时先确认同行人，再根据你所在平台给你推荐一个手机电脑都能打开的账本方案、由你确认；之后你只要自然语言报账，每笔都会同步并回执，记错了也能直接说"改刚才那笔"。准备好时说"我要去 XX 旅行，帮我记账"就行。

如果用户已经说了"我要去 XX 旅行，帮我记账"，跳过介绍，直接进开始流程。

## 开始旅行：渐进建账

从原话提取目的地、同行人、本人身份；已知的不重复问。每轮只处理一个主题，最多问两个紧密相关的问题。

1. **同行人**：缺才问"同行人都有谁？"。"我"天然是当前用户；用户说"我和 A"时不再追问谁是本人。只有名单全是姓名/昵称、无法判断本人时才问"这些名字里哪个是你？"。不主动问旅行日期，记账日期默认消息当天。

2. **从哪到哪（定 profile）**：判断国内还是国际。国内行到此不再问任何外币问题。国际行继续按 `references/foreign-money.md` 处理外币准备。中文提问且未说明出发地 → 默认从中国出发。

3. **账本载体**：见下节「载体：能力推荐 + 用户选择」。

4. **（仅国际行）外币现金 / 储值卡准备**：读 `references/foreign-money.md`。只在有真实金额时创建钱包余额，不虚构。

5. **建账并验证**：验证——底账可重读；用户账本可打开且有权限；成员和默认规则正确；一笔 AA 闭合；转移不计消费；净额和为零；无虚构消费或余额。测试数据验证后清除。

6. **交接四动作**，各给一句可照抄的话，然后邀请第一笔：
   - 记一笔：`A 付了 30000 韩元吃饭，四个人 AA。`
   - 看账：`现在每个人花了多少？`
   - 改错：`刚才那笔改成只有我和 A。`
   - 分账：`现在谁欠谁？`

   告诉用户：通常只要说清"谁付了、多少钱、买了什么、谁一起承担"；能按默认判断时无需补格式；漏了真正影响计算的信息，Agent 会追问。

## 载体：能力推荐 + 用户选择

默认把可分享的**在线表格**作为用户账本（手机电脑可看、可持续更新、可分享）。**先探测当前宿主真实能创建、能持久写入、用户能打开的能力**，给出**有依据的推荐 + 一个具体备选**，让用户选。不甩原始技术菜单，也不替用户拍板。

| 宿主 | 推荐方案 | 备选 |
|---|---|---|
| OpenClaw（飞书/微信通道，有 lark 能力） | 飞书多维表格，给可打开/分享链接（微信用户同样拿飞书链接） | 用户打不开飞书 → 腾讯文档在线表 / 可访问网页账本 |
| Coze | Coze 原生表格/数据库 + 可查看渲染 | 宿主可导出的在线文档 |
| WorkBuddy / 通用宿主 | 宿主自带的可持久可分享表 | 在线文档；再不行 → Agent 持账 + 每次刷新"账本快照"回执 + 随时可导出 |

选定后说清**平台 / 位置 / 归属 / 是否需授权 / 从哪打开**，用户确认或改选后才创建。

**红线**：JSON、数据库、应用私有文件**不得**冒充用户账本，也不能把文件路径当交付结果。用户账本同步失败时，本次**不能**回复"已记好"——说明未完成、保留可恢复状态、优先修复。只有当前宿主完全无法可靠创建/授权/持续同步，且没有任何用户能打开的方案时，才停止建账并如实说明。

## 日常记账

从原话提取：事项、原币金额、付款人、参与人或分摊方式、支付来源、日期。能按账本规则算清就直接记。只有缺失信息会**改变金额/付款人/参与人/换算**时才追问，一次只问最关键的一个（如：谁付款；谁参与或怎么分；币种；外币用现金/储值卡还是信用卡/线上）。

一条消息混共同+个人 → 拆多笔；多人各自付款 → 按各自付款拆多笔。

每笔按序：**回显**（不变量 1）→ 计算并校验闭合 → 写底账 → 同步用户账本 → 核对两边 → **回执**（含不变量 4 去重提示）。

回执示例：
```
已记：晚餐 ¥240
你付款｜你、A、B AA，各 ¥80
账本已更新
```

## 分摊与结算算法（无脚本宿主照此手算；有 python 用 scripts/trip_ledger.py 校验）

- **AA 平分**：`base = 金额 // 人数`，`余数 = 金额 % 人数`；余数每份 +1 个最小单位，先给参与的付款人，再按稳定成员顺序。
- **自定义金额**：各人指定额之和必须 = 消费金额，否则提示补差或确认差额归属。
- **个人消费**：承担人=指定个人、全额；付款人≠承担人则生成代付债务。
- **请客**：付款人承担全额，其他参与人承担 0，不生成债务。
- **各付各**：拆成每人一笔、各自付款各自承担、自动已结清、不生成债务。
- **净额**：`net(成员) = 该成员向商家付款 - 该成员应承担`。>0 应收，<0 欠款，全体和为零。
- **结算方案**：贪心——把最大欠款人配给最大应收人，取两者较小值转账，循环。仅**建议**，不自动入账；用户报告真实转账后才记结算（`net(转出)+=s, net(转入)-=s`）并重算。

## 修改与撤销

改账遵循不变量 2、3。唯一命中的**未结算**记录直接改、重算、同步、回执。多笔可能命中、批量修改、破坏性操作先确认。"撤销刚才那笔"标记作废、保留历史，不物理删除。不自动改已结算记录——先说明影响再问怎么处理。

## 查询与结算

区分四种口径，别混：消费总额 / 每人实际承担 / 每人向商家付款 / 成员间未结净额。用户问"我花了多少"先答个人承担，再补代付。问"谁欠谁"给最少转账方案但不自动入账。旅行结束给总消费+分类汇总+每人付款与承担+结算方案+账本入口。

## 何时加载 reference

- 国际行、或出现外币/换汇/储值卡/信用卡出账 → 读 `references/foreign-money.md`。
- 对话不确定该怎么处理（查账vs改账、混单、请客、各付各、补记）→ 读 `references/examples.md`。
- 有 python 环境要做确定性分摊/结算校验 → 用 `scripts/trip_ledger.py`。

## 宿主 Agent 职责

Skill 规定体验、逻辑账本和计算规则，不规定飞书/腾讯/Coze/本地/云端的工程实现。宿主必须：优先用已授权且能持续写入的在线表格；创建前说明并征得同意；保证用户可打开；每次记账/改账后同步；明确账本位置、归属、共享状态；不拿私有存储冒充用户账本。
